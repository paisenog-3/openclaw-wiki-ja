baseURL = 'https://paisenog-3.github.io/openclaw-wiki-ja/'
languageCode = 'ja-JP'
title = 'OpenClaw 日本語Wiki'
copyright = 'Maintained by <a href="https://x.com/OG3_gata" target="_blank">パイセン｜AIと暮らす</a>'

# Hextra テーマ設定（Hugo Module方式）
[module]
  [[module.imports]]
    path = "github.com/imfing/hextra"

# 言語設定
defaultContentLanguage = 'ja'
[languages]
  [languages.ja]
    languageName = '日本語'
    weight = 1
    [languages.ja.params]
      description = 'OpenClawの日本語Wiki - コミュニティで育てる実践ガイド'

# メニュー設定
[menu]
  [[menu.main]]
    name = "GitHub"
    url = "https://github.com/paisenog-3/openclaw-wiki-ja"
    weight = 1
    [menu.main.params]
      icon = "github"
  [[menu.main]]
    identifier = "search"
    name = "検索"
    weight = 2
    [menu.main.params]
      type = "search"
  [[menu.main]]
    identifier = "submit-article"
    name = "📝 投稿する"
    url = "https://github.com/paisenog-3/openclaw-wiki-ja/issues/new?template=article-submission.yml"
    weight = 3
    [menu.main.params]
      newWindow = true
  [[menu.main]]
    identifier = "theme-toggle"
    weight = 4
    [menu.main.params]
      type = "theme-toggle"

# Hextra パラメーター
[params]
  description = 'OpenClawの日本語Wiki - コミュニティで育てる実践ガイド'
  images = ['images/hero.jpg']

  # Navbar設定
  [params.navbar]
    displayTitle = false
    displayLogo = true
    [params.navbar.logo]
      path = "images/navbar-logo.png"
      dark = "images/navbar-logo-dark.png"
      width = 180
      height = 40

  # Page設定
  [params.page]
    # wide (90rem), normal (1280px)
    width = "wide"
    fullWidth = true

  # Theme設定
  [params.theme]
    # light | dark | system
    default = "system"
    displayToggle = true

  # 検索設定
  [params.search]
    enable = true
    type = "flexsearch"
    
    [params.search.flexsearch]
      # content | summary | heading | title
      index = "content"
      # full | forward | reverse | strict
      tokenize = "forward"

  # 編集リンク設定
  [params.editURL]
    enable = true
    base = "https://github.com/paisenog-3/openclaw-wiki-ja/edit/main"

  # フッター設定
  [params.footer]
    displayCopyright = true
    displayPoweredBy = true

# マークアップ設定
[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
  [markup.highlight]
    noClasses = false

# 出力形式
[outputs]
  home = ['HTML', 'RSS', 'JSON']
  page = ['HTML']
  section = ['HTML', 'RSS']

# セキュリティ設定
[security]
  enableInlineShortcodes = false
  [security.exec]
    allow = ['^(dart-)?sass(-embedded)?$', '^go$', '^npx$', '^postcss$']
    osEnv = ['(?i)^((HTTPS?|NO)_PROXY|PATH(EXT)?|APPDATA|TE?MP|TERM|GO\w+|(XDG_CONFIG_)?HOME|USERPROFILE|SSH_AUTH_SOCK|DISPLAY|LANG|SYSTEMDRIVE)$']
  [security.funcs]
    getenv = ['^HUGO_', '^CI$']
  [security.http]
    methods = ['(?i)GET|POST']
    urls = ['.*']
