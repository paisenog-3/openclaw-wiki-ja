name: "📝 ノートを投稿する"
description: "Wikiにノートを投稿します"
title: "[ノート投稿] "
labels: ["article-submission"]
body:
  - type: markdown
    attributes:
      value: |
        以下のフォームに記入してください。投稿後、自動でWikiに掲載されます。

  - type: input
    id: title
    attributes:
      label: ノートタイトル
      placeholder: "例: Cronジョブの設定方法"
    validations:
      required: true

  - type: dropdown
    id: category
    attributes:
      label: カテゴリ
      options:
        - セットアップ（setup）
        - スキル・プラグイン（skills）
        - 運用Tips（tips）
        - トラブルシューティング（troubleshooting）
        - リリースノート（releases）
        - その他
    validations:
      required: true

  - type: input
    id: author_name
    attributes:
      label: "投稿者名（任意 — 空欄の場合は「匿名」と表示されます）"
      placeholder: "例: たろう"
    validations:
      required: false

  - type: input
    id: author_link
    attributes:
      label: "SNS / Webサイト（任意 — ノートに投稿者リンクとして表示されます）"
      placeholder: "例: https://x.com/your_account"
    validations:
      required: false

  - type: textarea
    id: content
    attributes:
      label: ノートの内容
      description: "Markdown形式で書けます。見出し（##）や箇条書き（-）も使えます。"
      placeholder: |
        ## 概要
        ここに概要を書く...

        ## 手順
        1. まず...
        2. 次に...
    validations:
      required: true

  - type: textarea
    id: notes
    attributes:
      label: 補足・メモ（任意）
      placeholder: "参考: https://docs.openclaw.ai/..."
    validations:
      required: false

  - type: checkboxes
    id: terms
    attributes:
      label: 確認事項
      options:
        - label: この内容をWikiに掲載することに同意します
          required: true
